import { SvgIcon } from "@mui/material";
import { LiaCitySolid } from "react-icons/lia";

const TmCityIcon = (props: any) => {
    return (
        <SvgIcon {...props}>
            <LiaCitySolid/>
        </SvgIcon>
    );
};

export default TmCityIcon;
