import { SvgIcon } from "@mui/material";
import { GiPoliceOfficerHead } from "react-icons/gi";

const TmPoliceIcon = (props: any) => {
    return (
        <SvgIcon {...props}>
            <GiPoliceOfficerHead {...props}/>
        </SvgIcon>
    );
};

export default TmPoliceIcon;
